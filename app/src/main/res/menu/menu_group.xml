<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <item android:id="@+id/createGroup"
        android:title="Create New Group"
        app:showAsAction="never"
        tools:ignore="AppCompatResource"/>

    <item android:id="@+id/addParticipant"
        android:title="Add Group Participant"
        app:showAsAction="never"
        tools:ignore="AppCompatResource"/>

    <item android:id="@+id/itemMedia"
        app:showAsAction="never"
        android:title="Media, Links and Docs"/>

    <item android:id="@+id/groupInfo"
        android:title="Group Info"
        app:showAsAction="never"
        tools:ignore="AppCompatResource"/>

    <item android:id="@+id/searchMessage"
        android:title="Search"
        app:showAsAction="never"
        tools:ignore="AppCompatResource"/>

    <item android:id="@+id/clearGroupChat"
        android:title="Clear Chat"
        app:showAsAction="never"
        tools:ignore="AppCompatResource"/>

</menu>